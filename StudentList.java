import java.util.ArrayList;

/**
 * Contains a list of students
 * @author Caroline
 *
 */
public class StudentList 
{
	private ArrayList<Student> students;
	
	public StudentList(ArrayList<Student> students)
	{
		this.students = students;
	}
	
	/**
	 * Adds a student onto the list of students, checking to make sure the that student does not already exist.
	 * If the student does already exist then StudentAlreadyExistsException should apply.
	 * @param astudent
	 */
	//public void addStudent(Student astudent)
	
	/**
	 * Removes a given student(stated by their ID number) from the list of students as long as the student does exist.
	 * If the student does not exist then StudentNotFoundException should apply.
	 * @param numID
	 */
	//public void deleteStudent(int numID)

}
